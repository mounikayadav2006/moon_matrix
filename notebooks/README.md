# Notebooks

Optional space for exploratory analysis (e.g. testing new SIFT parameters,
inspecting sample-data generation) outside the Streamlit app. Empty by
default in this prototype — add `.ipynb` files here as needed.
